<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Temperature</title>
    <link rel="stylesheet" href="/css/temperature.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Knewave&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    
    <h1>Temperature 🌡️</h1>

    <form method="get" action="/historical/temperature">
        <label>Start: 
            <input type="date" name="start" value="<%= start || '' %>">
        </label>
        <label>End: 
            <input type="date" name="end" value="<%= end || '' %>">
        </label>
        <button type="submit"> Filter </button>
    </form>

    <% if (!start || !end) { %>
        <p class="message">Select a date range</p>
    <% } else if (labels.length === 0) { %>
        <p class="message">No data available in this range </p>
    <% } else { %>
        <div class="chart-container">
            <canvas id="tempChart"></canvas>
        </div>
        <script src="/js/temperature.js"></script>
        <script>
            renderTemperatureChart(<%- JSON.stringify(labels) %>, <%- JSON.stringify(data) %>);
        </script>
    <% } %>

     <a href="/historical" class="temp-btn">Back</a>

</body>
</html>