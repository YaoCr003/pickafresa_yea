<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Humidity</title>
    <link rel="icon" type="image/png" href="/img/gota-de-agua.png">
    <link rel="stylesheet" href="/css/humidity.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Knewave&display=swap" rel="stylesheet">
</head>
<body>

    <div id="loader">
        <div class="spinner"></div>
        <p>Cargando...</p>
    </div>

    <a href="/historical" class="back-btn">⬅ Back</a>

    <div class="rain"></div>

    <div class="container">
        <h1> Humidity </h1>

        <form method="get">
            <input type="date" name="start" value="<%= start || '' %>">
            <input type="date" name="end" value="<%= end || '' %>">
            <button type="submit">Filter</button>
        </form>

        <% if (message) { %>
            <p class="message"><%= message %></p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>Date</th>
                        <th>Ambient humidity (%)</th>
                        <th>Substrate moisture (%)</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(row => { %>
                        <tr>
                            <td><%= row.date %></td>
                            <td><%= row.ambient %></td>
                            <td><%= row.substrate %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>

    <script src="/js/humidity.js"></script>
    
</body>
</html>